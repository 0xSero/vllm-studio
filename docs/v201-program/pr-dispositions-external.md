# v2.0.1 External-Contributor PR Dispositions — Frozen Audit Ledger

GLM-5.3 read-only audit lane, 2026-08-15 UTC. Revised same day after Claude Opus-5 REVISE, r2 REVISE, and the r3 table-rendering review (corrections: #380 scope, #271 head SHA, #371/#395 test census, #395 doc/test line figures, validation section, audit-time head qualifier, #362 table-cell rendering). Scope: the 23 frozen fork-external PRs — #269, #271, #361–#380 inclusive, #395. Candidate track: **PR #408** (`feat/v201-consolidation` → `dev`, draft); `c0036a57d7e8c4d816d990bd0f9b1fc3a1f5fcbf` is its **audit-time integration head** and this branch's audit base (350 files, +10,713/−16,562 vs `dev` at audit time), not the live PR head. The live remote head is now `e8dacb6acb05b7755634c0d73b1e824f914a39fa`; §7 records the later accepted adaptations and supersedes the original cells for every named row. All 23 PRs were re-verified live (state, head SHA, base, metadata) and diffed head-vs-merge-base; equivalence claims cite code in the #408 tree, never titles.

**Coverage: 23/23 audited.** Every row below was derived from the PR's actual diff and a current-tree grep/read of the touched behavior.

**Closure gate (standing):** no PR here is declared safe to close. `fully-harvested-close-after-acceptance` is assigned to **zero** PRs today; every closure requires the row's acceptance evidence plus a fresh state check. Security/reliability rows are never marked superseded without line-level code evidence in the #408 tree.

### Census-precision note (post-REVISE)

Path-glob test census overcounts two ways and was corrected: `frontend/src/app/api/agent/connectors/test/route.ts` (#371) and `controller/src/modules/engines/engine-spec.ts` (#395) match `/test/` and `spec` patterns but are production source and stay in port scope. Verified figures: #371 = 1 executable test + 1 fixture; #395 = 27 test files / 1,134 added lines, docs = 1,041 lines (windows-support 177 + windows-port-plan 436 + windows-port-audit 428) + README +12.

## §1 Ledger (23 rows)

Legend — disp: **SP** = selectively-port, **SN** = superseded-no-port, **KD** = keep-open-dependency. ov = files also changed by #408 (textual-conflict surface). risk = omission risk class. All heads verified OPEN unless noted; base `dev` except #380 → `main`.

| PR | author | head SHA (full, live) | purpose (from diff) | ov | already in #408? (evidence) | unique to port | excl. (policy) | risk if omitted | disp | proof before close |
|---|---|---|---|---|---|---|---|---|---|---|
| 269 | Dixith-dev | `4bb29b450dec356373eeb77fafba1bb912aaec3e` | `mergeControllers` inherits saved API key for URL-matching requested controller (loopback aliases normalized) | 1 | No — current `pi-runtime-models.ts` dedupes by URL but never inherits `settings.apiKey`; keyless request stays keyless | key-inheritance + `controllerUrlIdentity` hunk (src only) | +1 test file | rel/med: saved controller credential silently dropped, agent auth fails | SP | ported hunk passes `npm run check`; live agent session loads models from saved controller without re-keying |
| 271 | Dixith-dev | `31df4f460afca9d17f6869c8347218dca71250bd` | workspace notices move below toolbar / clear of composer (`z-[110]`, `data-workspace-notices`) | 2 | No — current `agent-workspace-shell.tsx:295` still `bottom-3 right-3 z-30` | one className hunk + attr | +1 e2e spec edit | ui/low | SP | visual check in live browser with error + setup warnings visible |
| 361 | fettpl | `534df765f10705a4be95be18f58979a746c1ec55` | strict Effect-Schema booleans for `trust_remote_code` / `enable_auto_tool_choice` (reject `null`, `"true"`, `0`, …) | 1 (test only) | No — current `recipe-serializer.ts:68,234-240` still lenient `coerceBoolean`; `Boolean("false") === true` enables remote code | `booleanSetting` hunk (src) | +2 test files | **sec/high**: JSON string `"false"` coerces to enabled `trust_remote_code` | SP | POST /recipes with `trust_remote_code:"false"` → 400; valid booleans round-trip |
| 362 | fettpl | `9debc4de82e5518486847769348a4a65a3c11a4f` | runtime job types become semantic (`install`\|`update` only); platform backends reject install; cache cleared per run | 0 | No — `runtime-routes.ts:25` still accepts `download` and `inspect`; `engine-jobs.ts` runJob routes both into `runEngineInstall`, cuda/rocm always `runPlatformUpgrade` | contract `RuntimeJobType`, route schema, `runJobOperation` routing | +1 test file | rel/med + sec/low: "download"/"inspect" silently performs installs/mutations | SP | job `type:"download"` → 400; cuda install → explicit unsupported error |
| 363 | fettpl | `791fe1d210b057a5b905af5a93ed420cfb80ac78` | keyless-controller request-authority guard: Host/Origin must match `allowed_hosts`/`cors_origins`; new `request-authority.ts` | 3 | No — no `request-authority.ts`, no `allowed_hosts` in `env.ts`, no keyless guard in `security-middleware.ts`/`app.ts` | `request-authority.ts`, keyless middleware, env/.env.example | +2 test files | **sec/high**: keyless controller trusts Host header → DNS-rebinding/cross-site control of model lifecycle | SP | with no `api_key`, request with forged Host/Origin → 403; loopback normal use unaffected |
| 364 | fettpl | `5c925a67da078082f2f0618402666b038fe66b81` | immutable CI toolchain: SHA-pinned actions, exact runtimes, dependabot, `workflow-policy.mjs` pin gate wired into `check` | 2 | No — `ci.yml` still floats `actions/checkout@v7`, `setup-bun@v2`; no dependabot, no policy gate | dependabot.yml, workflow-policy.mjs (self-contained CLI), pinned workflow edits; a conforming port must keep only the CLI half (`node .github/workflow-policy.mjs`) of `check:workflow-pins` (its `node --test .github/workflow-policy.test.mjs` half is a test); semantic-release devDeps pins are optional (release.yml already pins via `--package`) | +1 test file; **generated bulk: `package-lock.json` +5,722 (94.8% of additions) — regenerate, never copy** | sec/med (supply chain): mutable action tags in CI | SP | `npm run check` (with ported CLI gate) green on re-pinned workflows; CI run green |
| 365 | fettpl | `650d6a6451cdd7b66b3665e0d40039b349788cab` | download target reservations: symlink-realpath, case/Unicode-normalized overlap lock across concurrent downloads | 1 (test only) | No — `downloads/` has only download-manager/store/api/backpressure; no reservation/lock symbol anywhere | `download-target-reservations.ts` + manager wiring | +2 test files | rel/med: concurrent downloads can clobber same target dir | SP | two overlapping-path downloads → second gets `DownloadTargetConflict` |
| 366 | fettpl | `d2dbdda8038ead0a7eb562b8e5e92ec62f86daea` | fail-closed frontend auth: prod without token/opt-out = 503; desktop requires loopback HOSTNAME; `/access` page + session route | 2 | No — `lib/auth/access.ts:41` still returns `allow/no-token`; no `/access` page, no `api/auth/session` | `access-posture.mjs`, guard 503, `/access` UI, desktop/project wiring | +1 test file | **sec/high**: production frontend (privileged agent surface) open by default | SP (product-gated) | prod build w/o token → 503; desktop build (env pairing landed) fully usable |
| 367 | fettpl | `0cd8dbf1b6157d6fec55b387c83ccd2712238383` | DNS-pinned browser policy + pinning proxy: private/link-local/multicast blocklists, all-answers-same-class, 5s resolve cap | 0 | No — `browser-host/` has no `network-policy.ts`/`pinning-proxy.ts` | both files + playwright/browser-host wiring | +1 test file | **sec/high**: embedded browser SSRF into LAN/loopback/host services | SP | nav to `http://127.0.0.1:*`/private IP blocked; public page loads |
| 368 | fettpl | `04c09dbb359581b201fc1cf2815fd271f56b412e` | unified hardened projects store: moves core from desktop to runtime+`shared/agent/projects.ts`, proper-lockfile, 0600/0700, atomic wx writes, migration marker | 5 | No — `frontend/desktop/logic/projects-store-core.ts` still the shared core; no `shared/agent/projects.ts`, no lockfile/0600 anywhere in store | `shared/agent/projects.ts` + rewritten runtime store (301 ln) | +1 test file edit | rel/med + sec/low: split-brain stores, symlink/permission exposure | SP | desktop + runtime agree on one 0600 `projects.json`; CRUD via both surfaces |
| 369 | fettpl | `757598a6dfd2fc09452945e8157dc99929ad3792` | atomic launch attempts: nonce-keyed cancel/release, `launchPrepared`, stale-attempt record protection | 3 | **Partial** — record `nonce` exists (`store.ts:266`) but `lifecycle.ts:81,127-140` still cancels/drops by **name**, not nonce | nonce-keyed `cancelRequested`, `store.release`, `launchPrepared` semantics | +2 test files | rel/med: stale cancel/cleanup of a superseded attempt kills the fresh launch of the same name | SP (remainder) | cancel fired during superseded attempt leaves the new record healthy |
| 370 | fettpl | `a9e2d4ff721bb0b51d6933dff4ae2bd222e9e840` | prove-orphan-ownership + hardened instance records | 2 (tests) | **Partial** — ownership proof IS present: `launchers/process.ts` `owns()` (startToken + `--port` cmdline fallback); lifecycle guards signals by ownership | remainder: `record-schema.ts` Effect-Schema decode (replaces loose `isRecord`), 0600/0700 + `wx` tmp writes, gated stop→drop | +2 test files | sec/med: poisoned/tampered record JSON accepted silently; record dir world-readable | SP (remainder) | malformed record file rejected loudly; `ls -l` shows 0600/0700 |
| 371 | fettpl | `72221172ddd31d5faccc779821f70574cf4a0e9b` | connector grants + human-approval broker: per-tool risk classes, catalog allowlists (github/x/computer), HMAC-digest approvals, 60s TTL | 2 | No — no `connector-policy.ts`/`connector-approval.ts`; no risk/grant/approval symbol in runtime | both files + pool/service/pi-runtime + agent API route wiring, incl. `connectors/test/route.ts` (+2 ln: exposes `connectorToolPermissions`) — production source, in port scope | +1 test file +1 fixture (census excludes `connectors/test/route.ts`: path contains `/test/` but it is shipped API source) | **sec/high**: agent mutates external SaaS (merge/push/tweet) with no human gate | SP | risky tool presents approval view; denial blocks execution; approval expires |
| 372 | fettpl | `9a94a40b4d3c74ae13401380431e0560824fccd6` | plugin artifact re-approval: bounded deterministic digest, connector-identity digest, execution snapshot leases, pool-state isolation | 1 | No — no `plugin-artifact-digest.ts`/`plugin-connector-identity.ts`/`plugin-execution-snapshot.ts`/`connector-pool-state.ts` | all four modules + runtime wiring | +4 test files | **sec/high**: plugin artifact swapped after approval silently reuses prior approval | SP | touching an approved artifact invalidates approval on next run |
| 373 | fettpl | `511ee85f94e4eac843e5c33f62e192f474a7d939` | unified browser surface: operation coordinator (per-kind timeouts 5–15s, recovery, semaphore), single ops queue | 7 | No — no `browser-operation-coordinator.ts`; UI panel files heavily reworked by #408 → rebase surface, not equivalence | coordinator + host/handler wiring (UI hunks need re-derivation) | +4 test files | rel/med: one hung browser op freezes the whole pane | SP | 15s verb timeout fires, recovery runs, pane stays responsive |
| 374 | fettpl | `79976b995acb5f6d9a485d59ddd185b1f3998210` | bounded MCP stdio framing: 4 MiB frame cap, newline reassembly, typed `McpProtocolError`s | 0 | No — `mcp-client.ts:67` uses raw SDK `StdioClientTransport` | `mcp-stdio-transport.ts` + client wiring | +2 test files (+fixture) | sec/med: rogue MCP server streams unbounded line → memory DoS | SP | server emitting >4 MiB frame → typed `frame-too-large`, runtime survives |
| 375 | fettpl | `a08af0f7a38da29061e232d2395b07147ec4351f` | browser session isolation: session-keyed contexts (max 8, idle 15 min, LRU), storage isolation, standalone session, runtime signal shutdown | 11 | No — no `browser-session.ts`/`session-request`/`session-surface`/`runtime-shutdown.ts`; head **moved** since census (d4b40a7→a08af0f) | session key schema/pool, ownership, `runtime-shutdown.ts` | **+16 test files** of 46; ~1.9k src additions | sec/med: cookies/storage/history shared across agent sessions (cross-session leakage) | SP (after 373) | two sessions get distinct isolated contexts; idle eviction reclaims; SIGTERM drains |
| 376 | fettpl | `ac9264d1aaefcac8719920216c2717480369542a` | explicit secret masking + hardened private file: mask exactly `secret_keys`, 0600/`wx` persistence, darwin/win ACL verify | 0 | **Partial** — heuristic `maskRecord` exists (`connectors-service.ts:25,170-187`) | explicit-key masking + `connector-private-file.ts` (339 ln) | +2 test files | sec/med: non-obvious secret-shaped keys leak in views; secret file readable by others | SP | view masks exactly declared keys; file mode 0600 on disk |
| 377 | fettpl | `c95c9e7e8ce44dacb6207bd40976feac8d5decab` | document + enforce semantic-release version authority for stable desktop releases | 2 | **Yes (code evidence)** — `project.mjs` `stageDesktopRelease` already asserts `metadata.version`/`localStudioCommit` vs release; `release.yml:20,46-56` already computes/propagates `next-release` version | nothing required; optional: fold version-authority paragraph into `docs/workflow.md` (its AGENTS.md edit conflicts with workflow.md-as-SoT) | inline node:test additions inside bundled `project.mjs` | rel/low (doc drift) | SN | none; if docs folded, workflow.md review |
| 378 | fettpl | `1f63b3f4c55413942db1deed616d543901687bd5` | redaction-focused hardening: escaped-JSON/multiline-aware `redactLogLine`, `process-boundary` (umask 077 + console patch), log-proxy route, launcher env redaction, installer hardening | 3 | No — current `log-redaction.ts` is the pre-#378 anchored-regex version; no `process-boundary.ts`/`log-proxy.ts` | all three core files + launcher/installer hunks (shares launcher files with #370) | +3 test files | **sec/high**: vLLM/SGLang env & JSON-escaped secrets streamed unredacted to UI/API | SP | log line with escaped-JSON secret → `[redacted]`; spawned env sanitized; umask 077 |
| 379 | fettpl | `ceb5946760de329e5479eeabf6d1ae108d61ac94` | GitHub MCP server artifact manager: pinned v1.6.0 read-only restricted-toolset binary, checksum verify, proper-lockfile, offline reuse | 2 | No — no `connector-artifacts.ts`, no `/connectors/github` route, no `GITHUB_MCP_*` symbols | artifact manager (1,201 ln src), route, credential UI (**shares #372's digest/identity modules — identical blobs; snapshot differs**) | +6 test files | sec/med: `npx -y` unpinned GitHub MCP server = mutable supply chain | SP (after 372) | artifact installs pinned version w/ checksum; reused offline; tools restricted |
| 380 | MarioMartinezII | `b8781a12b6c3bc78611dca0c4ef1293d09362709` | **controller-only** (no UI files in diff; live **CONFLICTING** vs `main`): merge enabled providers' models into `/v1/models` as `provider/model` ids with `resolveModelVision` flags, via new `services/provider-model-catalog.ts` + extraction in `provider-routes.ts` + `models/routes.ts` merge | 2 | **Partial (code evidence)** — `provider-routes.ts:70-92` `providerModels` fetch + `/studio/provider-models` route + `studio.ts:207` frontend client already present (near-identical impl); absent: the `/v1/models` catalog merge itself | the `/v1/models` merge (`provider/model` ids, `external`/`owned_by` metadata, vision flags) + catalog extraction into `services/provider-model-catalog.ts` | +1 test file | ux/low: configured providers stay invisible to OpenAI-compatible catalog consumers | SP (product-gated) | GET `/v1/models` lists `provider/<model>` entries for a configured enabled provider |
| 395 | JoaoZaokk | `1418b7496654ac0a9a69599ef6138a95fe269a38` | Windows 11 x64 platform port (draft): WSL2 bridge runtime, native llama.cpp, PowerShell installer, platform shims | 28 | No (different axis; #408 is not a Windows port) | required code/config: `wsl-managed-runtime.ts` (1,053), `wsl2.ts` launcher (294), `process-platform.ts` (173), `install-controller.ps1` (258), `python-venv-path`, `platform-shell`, `engines/engine-spec.ts` (production source), `icon.ico`, builder/next config. **Bulk: docs 1,041 ln across the three Windows docs + README +12; tests 1,134 ln across 27 files — excluded** | +27 test files; docs as reference only | platform-gap (accepted short-term) | KD | platform-strategy adjudication; if pursued: rebase plan over #408's converged controller + green Windows CI |

## §2 Prioritized port queue

Security-first ordering with sequencing constraints (shared-file and module dependencies):

1. **#363** keyless authority guard — highest unauthenticated exposure on the controller.
2. **#366** fail-closed frontend (product-gated: needs desktop env pairing decision first).
3. **#367** browser network policy + pinning proxy.
4. **#361** strict recipe booleans (smallest high-severity hunk; clean vs #408 — src file untouched by #408).
5. **#378** redaction/process-boundary (sequence with #370 — both rewrite `launchers/process.ts`/`docker.ts`).
6. **#374** bounded MCP stdio framing (zero overlap).
7. **#371** connector grants + approval broker.
8. **#372** plugin artifact digest/identity/snapshot → **#379** GitHub MCP artifact manager (shares #372's modules; identical digest/identity blobs verified).
9. **#376** explicit masking + private file (zero overlap).
10. **#365** download target reservations.
11. **#369** nonce-keyed cancel/release remainder.
12. **#370** record-schema + permission remainder (after #378 if sequenced together).
13. **#368** unified projects store.
14. **#373** browser operation coordinator → **#375** session isolation (after #373; 11-file UI overlap with #408's newer panel requires re-derivation, not textual cherry-pick).
15. **#362** semantic runtime job types.
16. **#364** CI pinning (regenerate lockfile; decide root-lockfile posture — #408 tree has no root `package-lock.json`).
17. **#269** controller credential inheritance.
18. **#271** notice layering.
19. **#380** `/v1/models` provider-catalog merge (product-gated; controller-only remainder).
20. **#377** superseded — optional docs fold only.
21. **#395** keep open — no port into the convergence track now.

## §3 Unresolved items

1. **#366 posture decision** — flipping the production default to fail-closed requires desktop wiring (`LOCAL_STUDIO_DESKTOP=1` + loopback `HOSTNAME` check) to be agreed and landed with it; otherwise the desktop app bricks in production mode.
2. **#380 product call** — the fetch layer (`/studio/provider-models` + frontend client) already landed in #408; the open decision is whether #380's controller-only `/v1/models` catalog merge (`provider/model` ids, vision flags) belongs in v2.0.1. PR is live CONFLICTING against `main`.
3. **#364 lockfile posture** — the repo (dev and #408) deliberately lacks a root `package-lock.json`; adopting the PR's lockfile is a dependency-management decision, not a mechanical copy.
4. **#373/#375 rebase depth** — both predate #408's browser panel rework; the depth of drift (like #407's automation runner) is mapped only at file level, not API level.
5. **#375 head drift** — live head `a08af0f7` ≠ census `d4b40a7`; any future action must re-verify head again.
6. **#395 platform strategy** — Windows support is unadjudicated; the port targets the pre-#408 controller (28-file overlap) and would need a funded rebase plan.
7. **#377 residual docs** — version-authority paragraph conflicts with `docs/workflow.md` as single source of truth; fold decision belongs to workflow.md's owner.
8. **Zero closures now** — no PR carries acceptance evidence yet; `fully-harvested-close-after-acceptance` count is 0/23 and must stay 0 until ports land and are accepted.

## §4 Validation record

All commands run in this worktree on the exact content committed; no claim below precedes its command finishing.

- **Audit commit `4929ba9af`** (`docs(v201): audit 23 external contributor PR dispositions against 408 track`): `git diff --check` clean (0 whitespace errors); `npm run check` **exit 0** — all six stages green (`check:automation`, `check:contracts`, `check:structure`, `check:frontend` incl. production `next build` + `complete-standalone` + `assert-standalone`, `check:controller`, `check:agent-runtime`; postbuild rewrote 170 specifiers). Transcript `/tmp/praudit/npm-check-run4.log`, exit line `NPM-CHECK-EXIT:0`.
- **Environment repair (pre-existing, docs-unrelated)**: the worktree's `frontend/node_modules` was a symlink into the primary v201 worktree; Next standalone re-emitted it as a symlink, so `complete-standalone`'s `cpSync` failed `ERR_FS_CP_EINVAL` (src≡dest) under both node v26.4.0 and v22.22.3 (transcripts `npm-check-run2.log` and `npm-check-run3.log`, exit 1; stale APFS-cloned `.next/standalone` from an Aug-13 run had the same inode as source). Fix scoped to this worktree: real `npm ci` (1,644 pkgs, exit 0) replacing the symlink + cleared ignored `.next` output. Primary worktree untouched.
- **Correction commit `f52489ee8` (first Opus-5 REVISE fixes)**: `git diff --check` clean; `npm run check` **exit 0** on the corrected content (stale `​.next/standalone` cleared first). Transcript `/tmp/praudit/npm-check-run5.log`, exit line `NPM-CHECK-EXIT:0`.
- **Later docs-only revisions `47f649966`, `da8fdf296`, and `341368243`**: content deltas are markdown-only (review corrections, audit-time qualification, and table rendering). No executable content changed, so per the repo's docs-only-after-green-gate convention the run5 transcript remains the operative gate evidence; its exit line `NPM-CHECK-EXIT:0` was re-read from disk for this record. Each revision passed `git diff --check` before commit.

## §5 Method (commands, no secrets)

- Live metadata: `gh pr view <N> --repo sybil-solutions/local-studio --json number,title,state,isDraft,baseRefName,headRefName,headRefOid,author,additions,deletions,changedFiles,mergeable` for N ∈ {269, 271, 361…380, 395, 408} (read-only).
- Read-only head fetch: `git fetch origin pull/<N>/head:refs/remotes/pr-audit/<N>`; heads resolved to full SHAs via `git for-each-ref refs/remotes/pr-audit`.
- Base anchoring: `git merge-base origin/dev pr-audit/<N>` (origin/dev `a765eb27b…`, origin/main `eeeb3406d…` — match frozen baselines; all fettpl PRs and #408 sit exactly on the dev tip, so diffs are clean).
- File census: `git diff --name-status/--numstat <merge-base> pr-audit/<N>`.
- Overlap: `comm -12` of each PR's changed-path set against #408's 350-path set (vs its own dev merge-base).
- Equivalence: file-existence probes (`git cat-file -e pr-audit/408:<path>`) plus targeted reads/greps of the #408 tree at the exact functions/identifiers each PR changes (locations cited per row).
- Generated-vs-required split: per-file numstat (e.g. #364 lockfile 5,722/6,035 = 94.8% of additions; #395 docs+tests vs platform code).
- Test census: path filter over `files-<N>.txt` (test/spec/fixtures), plus inline-test inspection for bundled `project.mjs` (#377).

## §6 Provenance and harvest handling

Authors: Dixith-dev (#269, #271), fettpl (#361–#379), MarioMartinezII (#380), JoaoZaokk (#395). All 23 are fork-external, all OPEN, none merged. At the frozen audit point every harvest was future work; §7 now records the accepted adaptations. Any further harvest follows repository rules: branch from `dev`, one agent one branch, PR into `dev`, conventional commits, `npm run check` green, never bypass hooks; ports are conforming cherry-picks/re-implementations that preserve external authorship (original-author attribution / `Co-authored-by` per repo convention), exclude every added test file listed above, and leave GitHub PR state untouched until acceptance evidence exists and closure is explicitly authorized. The frozen audit used ephemeral metadata, file lists, and diffs under `/tmp/praudit/`; that path is no longer present and is not durable evidence.

## §7 Security and reliability adjudication checkpoint — 2026-08-15

This append-only checkpoint supersedes the original disposition and queue cells only for the rows named below. The frozen census remains historical evidence. Live metadata was refreshed against GitHub on 2026-08-15; no contributor PR was closed, edited, reviewed, or otherwise mutated.

### Accepted selective adaptations

| Source PR | Accepted boundary | Canonical evidence | Residual boundary |
|---:|---|---|---|
| 269 | Matching loopback controller requests inherit the current saved Settings credential in memory; explicit credentials remain authoritative; unrelated controllers remain keyless | product `8a653d082` plus rotation-safety repair `6f5c77a6d`; evidence `bd380f108` | The initial adaptation persisted inherited keys and failed review. The repaired slice persists only normalized original entries, so Settings-key rotation is observed on the next refresh. Exact manual credential probes and package/static gates pass, and all nine hosted contexts are green at exact encompassing PR #408 head `e8dacb6ac`; installed agent-session proof remains open. The slice adds 27 frozen product-code lines in the runtime file. |
| 271 | Workspace notices move above the composer and below the toolbar, with `z-[110]` and `data-workspace-notices` | product `fcf57a9d9`; evidence `bd380f108` | Frontend static gates and all nine hosted contexts at exact encompassing PR #408 head `e8dacb6ac` pass. The placement still requires live narrow-window, active-composer, toolbar, and popover visual acceptance and an installed build. The slice adds 3 frozen product-code lines. |
| 361 | Strict recipe booleans, production serializer only | product `675b79f58`; final integration ledger `fa5fe8f95` | Installed UI and legacy malformed-row presentation remain acceptance work. |
| 362 | Semantic `install`/`update` runtime-job contract and dispatch plus truthful platform-job backend/command presentation | reviewed contract product `f5af3284b`; canonical contract `b314dce36`; metadata repair `2977b63c2`; evidence `9bfe31d28` + `cdead91f2` | Raw PR remains held because it adds a prohibited automated test. The prior P2 is fixed: CUDA/ROCm jobs retain their real backend, unsupported platform installs expose no fictional command, and update plus managed-engine paths are unchanged. Manual loopback behavior and the combined exact-tree gate pass; all nine hosted contexts are green at exact encompassing PR #408 head `e8dacb6ac`, while installed acceptance remains open. |
| 363 | Keyless Host/Origin authority plus strict authority syntax | product `d07b5b3e9` + `4988cffaf`; evidence through `b7b73e9ab` | Installed and release proof remain open. |
| 366 | Fail-closed production frontend access with desktop loopback posture | product `5ffaf8c49`; evidence `edd106d2c` | Shared-secret cookie, login-specific rate limiting, and installed acceptance remain explicit gaps. |
| 367 + 373 | DNS-pinned browser transport plus composed operation lifecycle, cancellation, recovery, and terminal-dot policy | canonical browser composition through product `1009d435d`; evidence `3f173e3f8` | Session isolation, installed visible-panel proof, Windows/Linux, and combined release proof remain open. |
| 374 | Bounded MCP stdio framing, detachable pending rejectors, sticky terminal state, and one shared idempotent shutdown | reviewed candidate product `f03488fdb`; canonical product `d4a30dd69`; evidence `7266933d0` | This adds 251 frozen product lines. Child environment, artifact identity, sandboxing, and installed/plugin proof remain open. |
| 378 | Controller logger and five patched console methods only, with linear fail-closed value scanning and equal console/file/event rendering | reviewed candidate product `cf46b92a4`; canonical product `6eb126b5d`; evidence `4dcd2b447` | Raw service/child stdout and stderr, direct process/file writes, desktop output, existing logs at rest, and launcher/installer hardening are not covered. |
| 370 | Only removal of the unproved pre-launch `docker rm -f` | canonical `68559e4d8`, externally attributed | The seven-file ownership protocol remains held; same-name foreign containers now fail launch rather than being destroyed. |

The #374 and #378 raw PR heads are not accepted substitutes for the reviewed adaptations. #374 required the lifecycle repair beyond raw head `79976b995`; #378 required the descendant through `cf46b92a4`, and the evidence ledger explicitly rejects its intermediate candidates.

### Current HOLD and rework dispositions

| PR | Live head | Disposition | Primary blockers |
|---:|---|---|---|
| 364 | `5c925a67d` | HOLD raw; audited only and absent from this checkpoint | The raw PR adds a prohibited automated test and generated root lockfile. A conforming current-tree CI pinning adaptation remains separate implementation and validation work; no #364 product bytes are accepted here. |
| 365 | `650d6a645` | HOLD raw and three-file production set | Pending metadata work can launch after shutdown; lexical reservations miss symlink/hardlink/volume aliases; locks are process-local and volume case semantics are inferred. |
| 368 | `04c09dbb3` | HOLD raw store rewrite; conditional GO only for a re-derived authority-only precursor | Lock lease can admit simultaneous writers, migration is CWD/path based and resurrection-prone, persistence lacks no-follow/fsync/Windows ACL proof, and raw composition breaks the canonical session watcher. |
| 369 | `757598a6d` | HOLD raw nonce-attempt port | Public stop remains name-only; cancellation is process-local; supervisor and placement-lock races release live ownership; store defects/interruption can orphan work; latest #370 semantics are not composed. |
| 370 | `bd073c809` | HOLD raw seven-file ownership protocol | No legacy-record migration, unknown owners can become ready, PID-only child tracking can kill replacements, non-Linux process trees/restart recovery are unsafe, started-reference coverage and nonce-CAS cleanup are incomplete. |
| 371 | `72221172d` | HOLD; no safe current execution subset | Caller-controlled mutation can self-grant tools, grants are not artifact-bound, discovery launches before approval with broad environment, mutable `npx` specs remain, and token-required frontend inventory is incomplete. |
| 372 | `e3eb719a7` | HOLD raw and 17-file production set | One malformed plugin disables all connectors; unbounded global discovery/hash work precedes limits and serializes admissions; env filtering is a loader blocklist; same-UID snapshots/grants are mutable; traversal, collision, crash/orphan, and durable-private-store gaps remain. Raw composition also overwrites accepted #374 and restores removed TTS capability. |
| 376 | `ac9264d1a` | HOLD private-file implementation | Windows fails closed, same-UID children can read plaintext, startup readers bypass the helper, rename/link/fsync/cross-process-lock/migration gaps remain, and args/query secrets stay exposed. |
| 379 | `ceb594676` | HOLD whole artifact manager | It pins stale GitHub MCP v1.6.0 built with Go 1.25.0, has stale-config/orphan/promotion/TOCTOU defects, embeds obsolete partial #372, and does not resolve #371/#376 authority and secret boundaries. |

PR #362 at live head `9debc4de8` remains **HOLD raw** because the raw PR includes a prohibited 245-line automated test. Its independently reviewed selective adaptation is recorded in the accepted table above, and the bounded platform-job presentation P2 is now resolved by canonical product `2977b63c2` with evidence `cdead91f2`.

All live heads in the two tables were OPEN, non-draft, and MERGEABLE but BLOCKED at refresh time, with no submitted reviews or successful hosted check runs. PR #372 exposed one `action_required` suite and zero registered check runs. These metadata facts are not source acceptance.

### Corrected sequencing

1. Preserve accepted #374 framing as the MCP base and accepted #378 controller-sink boundary as a bounded slice.
2. Carry the completed #362 platform-job presentation repair through installed runtime-job acceptance; exact encompassing PR #408 head `e8dacb6ac` has passed all nine hosted contexts and no source P2 remains in the selective slice.
3. Rebuild a cross-platform private-file primitive before connector grant or artifact work.
4. Resolve #371 grant authority before a corrected #372 digest/identity/discovery/snapshot lifecycle; rebase #379 last and retain only unique artifact-manager behavior.
5. Rebuild #370 ownership with versioned legacy reconciliation, tri-state inspection, exact process-tree containment, typed store Effects, durable CAS, and crash recovery; compose corrected #369 nonce admission afterward.
6. Re-derive #368 authority first, then repair its migration/lock/private-file store atomically with the canonical session watcher.

No row in this addendum authorizes PR closure. Closure still requires accepted integration, current CI, applicable installed/manual evidence, and a fresh unique-commit/state check.
