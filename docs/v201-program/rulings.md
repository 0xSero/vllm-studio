# Rulings Digest (G0A–G0H2)

Compact digests of the Fable-5 adjudications that govern this program. Full text lives in the Fable plan authority; this file is the in-repo digest.

- **G0A — Safety net.** GLM-A1 additive backup approved: status (v1/v2), unstaged/staged patches, untracked copy + checksums, before-checksums (tracked+untracked), `git bundle --all` (verified self-contained), collision-safe annotated archival tags for every branch head / worktree head / stash commit, full ref/PR/worktree/stash inventory. All additive; no deletion. Steering later corrected: never enumerate/checksum *ignored* files (two interrupted ignored manifests marked INCOMPLETE); recovery scope = tracked checksums + binary patches + non-ignored untracked copies + bundle.
- **G0B — Hook merge exemption.** The repo's own pre-commit hook caps authored commits at 15 active files / 600 source lines, which structurally blocks merge commits. Exemption implemented in `frontend/desktop/project.mjs` (the physical target of the `scripts/project.mjs` symlink): a new `isMergeInProgress()` detector (true only when `MERGE_HEAD` resolves for this worktree) guards the cap as `!isMergeInProgress() && (activeFiles > 15 || lines > 600)`. Every normal authored commit remains capped; all other hook behaviors byte-identical. Server-side GitHub merge explicitly **not** used.
- **G0C — Cap scope finding.** `docs/**` lines **do** count against both thresholds. The commit partition (R20) is sized for it: each commit ≤12 files / ≤550 added lines (deliberate margin under 15/600).
- **G0D — Frontend repair.** `frontend/desktop/logic/frontend-restart.test.ts` (62 lines) deleted — a merge-induced TS2307 (`bun:test` import type-checked after main's tsconfig dropped the `**/*.test.ts` exclusion). No-tests rule satisfied. Pre-existing-files-defer line held except the explicit Inkling exception.
- **G0E — Phase-0 close-out packet.** V1 hook proof accepted (project.mjs vs `eeeb3406` = 2 hunks, +8/−1, MERGE_HEAD detector + cap guard only). Inkling test disposition (R14): `services/agent-runtime/test/inkling-thinking-levels.test.ts` deleted via DeepSeek lane `ds/remove-dead-inkling-test` → GLM `--ff-only`; lane removed zero-residue. R12c sweep refined: match test-file basename globs (`*.test.*`/`*.spec.*`) + content, never directory name alone; `connectors/test/route.ts` allowlisted (live POST route, path segment). Publication packet P1–P10 defined.
- **G0F — Ledger & manifest specification.** R16 heads (H0=`dcb790fd` immutable; docs-only after; manifests measure `eeeb3406`/`a765eb27`/H0, never Hpush). R17 durable backup relocation. R18 canonical ledger tree (filenames frozen). R19 deterministic manifest definitions (LOC/size/routes/pages/tables/PR/branch/worktree). R20 cap-safe commit partition. R21 external-vs-in-repo. R22 P5 gate mechanics.
- **G0G — Routes extraction spec; registrar correction.** C14: at `eeeb3406`, `app.ts` imports **eight** register functions (incl. audio, speech); the dossier's "six registrars / no audio module" map was read at `262f84c7` on the *unmerged* #403 lineage — differences are expected. R23 replaces R19's "six registrar files" phrasing: whole-tree static scan, frozen pattern set P, literal + `<dynamic>` rows, wiring chains, classification enum, mandatory honesty header; dossier/`controller.md` as cross-checks only; optional runtime corroboration is DeepSeek/Phase-2.
- **G0H — PR census semantics (30-vs-29).** R24: canonical = durable 29-row snapshot; frozen append-only. R26: R26 columns + owner-class from snapshot fields.
- **G0H2 — Census correction.** The historical "30" = prose arithmetic, not enumeration: the dossier's table carries 29 rows (6 maintainer + 23 fork) while prose claims 24 fork. Offline set-diff of the recovered 29-number list vs the snapshot 29 = **EXACT MATCH**. R25′: record `unresolved-historical (one fork PR, number unknown, per prose arithmetic)`; the narrow 18:26–19:03Z closed-PR archaeology probe came back **empty** → `unresolved-benign` (prose miscount best-supported). Current-state sanity check = 29 open, identical, no drift. The 29-row frozen census stands untouched. Durability lesson: load-bearing evidence serialized into plan/ledger at ruling time (session JSONLs are volatile).
